require "test_helper"

class Event::DataCompletenessTest < ActiveSupport::TestCase
  # test "ticketmaster events should be data completed" do
  #   city_source = city_sources(:ticketmaster)
  #   events_attributes = city_source.scrape
  #   events = city_source.events.build(events_attributes)

  #   events.each do |event|
  #     assert event.data_completed?
  #   end
  # end
end
