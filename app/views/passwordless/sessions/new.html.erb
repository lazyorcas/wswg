<% content_for :title, "Passwordless Login | Where Should We Go" %>
<div class="grow flex flex-col justify-center items-center gap-y-8 w-full">
  <h1 class="text-3xl font-bold">Passwordless Login</h1>
  <div class="w-full md:max-w-100">
    <%= render "form", session: @session %>
  </div>
</div>
