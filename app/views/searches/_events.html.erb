<div class="flex flex-col gap-y-2 pt-4">
  <p class="text-neutral-500 text-sm px-4">
    Found <%= event_count %> events
  </p>
  <section class="flex flex-col">
    <% events.each do |event| %>
      <%= render Map::EventComponent.new(event) %>
    <% end %>
  </section>
</div>