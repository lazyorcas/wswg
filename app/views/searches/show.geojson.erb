{
  "type": "FeatureCollection",
  "features": [
    <% @located_events.map.with_index do |event, index| %>
    {
      "type": "Feature",
      "properties": {
        "dom_id": "<%= dom_id(event) %>",
        "url": "<%= event_path(event, context: "map") %>"
      },
      "geometry": {
        "type": "Point",
        "coordinates": [ <%= event.location.longitude %>, <%= event.location.latitude %> ]
      }
    }<%= "," if index < @located_events.length - 1 %>
    <% end %>
  ]
}