<main class="w-full">
  <h1 class="text-3xl font-bold text-neutral-800 mb-2">Where Should We Go?</h1>
  <%= render "form", search: Event::Search.new(query: @search.query) %>
  <% if @events.present? %>
    <div class="mt-10">
      <%= render "events", events: @events, search: @search %>
    </div>
  <% end %>
  <%= render "shared/flash" %>
</main>