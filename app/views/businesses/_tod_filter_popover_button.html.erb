<%= tag.button popovertarget: "tod_filter_popover", class: class_names("icon-button icon-button--xs", filtering_by_tod? ? "button--active" : "button--ghost"), title: "Filter by Time of Day" do %>
  <%= tag.i class: class_names("ph-funnel", filtering_by_tod? ? "ph-fill" : "ph") %>
<% end %>
<div popover id="tod_filter_popover" class="modal p-4 w-xs max-w-full">
  <div class="flex flex-col">
    <p class="mb-4 text-lg font-bold text-primary">Time of Day</p>
    <%= form_with url: business_events_path, method: :get, html: { class: "flex flex-col gap-y-4" } do |form| %>
      <% event_query_params.each do |key, value| %>
        <%= form.hidden_field key, value: value %>
      <% end %>
      <%= form.select "tod", options_for_select(tod_options), { include_blank: "All" }, { class: "input input--accent" } %>
      <%= form.submit "Filter", class: "button button--primary" %>
    <% end %>
    <% if filtering_by_tod? %>
      <%= form_with url: business_events_path, method: :get, class: "mt-2" do |form| %>
        <% event_query_params.except(:tod).each do |key, value| %>
          <%= form.hidden_field key, value: value %>
        <% end %>
        <%= form.submit "Clear filter", class: "w-full button button--ghost" %>
      <% end %>
    <% end %>
  </div>
</div>