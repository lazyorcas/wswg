<%= turbo_stream.update "event-bottom-sheet" do %>
  <button class="absolute top-2 right-2 icon-button icon-button--small button--ghost" data-action="bottom-sheet#emptyBottomSheet">
    <i class="ph ph-x"></i>
  </button>
  <%= render Event::Map::InfoWindowComponent.new(@event) %>
<% end %>