<% flash.each do |key, value| %>
  <%= tag.p value, class: class_names("flash", {
    "flash--info": key == "info",
    "flash--success": key == "success",
    "flash--warning": key == "warning",
    "flash--error": key == "error"
  }), data: { flash_target: "message" } %>
<% end %>