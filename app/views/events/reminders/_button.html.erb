<%= turbo_frame_tag "#{dom_id(event)}_reminder", src: event_reminder_path(event), loading: "lazy" do %>
  <button class="w-full button button--disabled" disabled>
    <i class="ph ph-bell-simple"></i>
    Remind
  </button>
<% end %>