<nav class="flex flex-col md:flex-row md:gap-x-4">
  <div class="flex flex-col md:flex-row gap-y-2 md:gap-y-0">
    <%= link_to "About", "https://www.linkedin.com/company/where-should-we-go/", target: "_blank", class: "button button--ghost" %>
  </div>
  <% unless signed_in? %>
    <hr class="border-primary my-4 md:hidden" />
    <div class="flex flex-col md:flex-row gap-y-2 md:gap-y-0 md:gap-x-2 md:border-l md:pl-4 border-primary">
      <%= link_to "Login", login_path, class: "md:flex-1 button button--ghost" %>
      <%= link_to "Sign up", new_user_path, class: "md:flex-1 button button--secondary" %>
    </div>
  <% end %>
</nav>