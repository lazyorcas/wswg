<nav class="flex flex-col md:flex-row md:gap-x-4">
  <div class="flex flex-col md:flex-row gap-y-2 md:gap-y-0">
    <%= link_to "About", "https://www.linkedin.com/company/where-should-we-go/", target: "_blank", class: "button button--ghost" %>
    <%= link_to "Pricing", pricing_path, class: "button button--ghost" %>
  </div>
  <hr class="border-primary my-4 md:hidden" />
  <% if signed_in? %>
    <%= link_to map_path, class: "button button--primary", data: { turbo: false } do %>
      <i class="ph-fill ph-sparkle"></i>
      Discover
    <% end %>
  <% else %>
    <div class="flex flex-col md:flex-row gap-y-2 md:gap-y-0 md:gap-x-2">
      <%= link_to "Login", login_path, class: "md:flex-1 button button--secondary" %>
      <%= link_to map_path, class: "md:flex-1 button button--primary", rel: "nofollow", data: { turbo: false } do %>
        <i class="ph-fill ph-map-trifold"></i>
        Open map
      <% end %>
    </div>
  <% end %>
</nav>