<div class="dark:bg-stone-800">
  <div class="h-dvh max-h-dvh flex flex-col container mx-auto px-4">
    <div class="py-4 md:py-8 flex flex-col justify-between h-full">
      <div class="grow flex flex-col gap-y-4 md:gap-y-8 justify-center">
        <header class="flex flex-col md:gap-y-1">
          <h1 class="text-3xl md:text-5xl font-bold">Where Should We Go</h1>
          <p class="text-sm md:text-base">Find all local events in your city with one search.</p>
        </header>
        <div class="w-fit">
          <% if signed_in? %>
            <%= link_to map_path, class: "button button--primary" do %>
              <i class="ph-fill ph-sparkle"></i>
              Find events
            <% end %>
          <% else %>
            <%= render "sessions/form" %>
          <% end %>
        </div>
      </div>
      <div class="shrink-0">
        <%= render "shared/footer" %>
      </div>
    </div>
  </div>
</div>
